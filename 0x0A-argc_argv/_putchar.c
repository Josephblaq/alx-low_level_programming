#include <stdio.h>
#include "main.h"
/**
 * _putchar - prints character c to the standard output (stdout)
 * @c: The character to be printed
 *
 * Return: Always return 0(success)
 */
int _putchar(char c)
{
return (0);
}

