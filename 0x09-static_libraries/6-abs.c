#include "main.h"
/**
 * _abs - computes the value of an int
 * @n: integer to be checked
 *
 * Return: return the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
