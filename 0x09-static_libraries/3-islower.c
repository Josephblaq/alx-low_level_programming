#include "main.h"
/**
 * _islower - searches for lowercase chars
 * @c: the char to be searched for
 * Return: 1 if c is a lowercase char, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
